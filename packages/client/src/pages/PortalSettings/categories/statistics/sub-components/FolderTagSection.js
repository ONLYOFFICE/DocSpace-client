import React from "react";
import { StyledFolderTagSection } from "../StyledComponent";

const FolderTagSection = (props) => {
  return <StyledFolderTagSection {...props} />;
};

export default FolderTagSection;
